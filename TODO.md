# TODO

- [x] Remove OpenAI/chatbot route `/ask_ai` and `/health_ai` and related UI/template usage
- [x] Remove `client = OpenAI(...)` initialization and related imports from `app.py`
- [x] Update navigation in templates to remove the AI assistant link/button (e.g., `index.html`)
- [ ] Verify app still runs (Flask) and prediction routes work
- [x] Run a quick grep to ensure no leftover chatbot/AI-assistant endpoints remain



